{
  "name": "@hschulz/domain-verify",
  "description": "Verify a domain using DNS or HTTP",
  "author": "Hauke Schulz <hauke27@gmail.com>",
  "license": "MIT",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build-lib && npm run build-bin",
    "build-lib": "tsc -p tsconfig-lib.json",
    "build-bin": "tsc -p tsconfig-bin.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^5.5.4"
  }
}
